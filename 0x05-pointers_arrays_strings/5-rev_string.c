#include "main.h"

/**
 * rev_string - reverse a string
 * @s: string
 *
 * Return: void
 */
void rev_string(char *s)
{
	int i, j;
	char *rs;

	i = 0;
	j = 0

	while (s[i] != '\0')
		i++;
	while (i != 0)
	{
		i--;
		rs[j] = s[i];
		j++;
	}
}
