#include <stdio.h>

/**
 * main - prints the alphabet in lowercase and uppercase,
 *
 * Return: 0
 */
int main(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(ch);
	for (c = 'A'; c <= 'Z'; c++)
		putchar(ch);
	putchar('\n');
	return (0);
}
