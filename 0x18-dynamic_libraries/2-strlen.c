#include "main.h"

/**
 * _strlen - retruns the length of a string
 * @s: pointer to a string
 *
 * Return: the size of the integer
 */
int _strlen(char *s)
{
	int i = 0;

	for (i = 0 ; s[i] != '\0' ; i++)
	{
	}

	return (i);
}
